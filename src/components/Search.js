import React, { Component } from 'react'

// <Search onSearch={ this.props.searchAction } > Clique Aqui <Search>

export default class Search {

	render() {
		/*return (
			<input />
			<button onClick={ this.props.onSearch } >{ this.props.text }</button>
		)*/
	}

}
